// Retorna um boleano verificando se pelo menos um item de um array satisfaz a condição

const arr = [1, 3, 3, 4, 3]

const hasSomeEvenNumber = arr.some(value => value % 2 === 0);
// true

// ai quero saber se algum valor do array é par, assim ele volta true ou false