// Ternário
condicao ? valor1 : valor2

true ? 'foo' : 'bar'        // Retorna 'foo'
false ? 'foo' : 'bar'       // Retorna 'bar'

// se a condição for verdadeira ela recebe o primeiro valor, se não recebe o segundo.
